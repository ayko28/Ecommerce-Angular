import { PromotionProduct } from './promotion-product.model';

describe('PromotionProduct', () => {
  it('should create an instance', () => {
    expect(new PromotionProduct()).toBeTruthy();
  });
});
