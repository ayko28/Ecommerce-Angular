import { FilterBySellerPipe } from './filter-by-seller.pipe';

describe('FilterBySellerPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBySellerPipe();
    expect(pipe).toBeTruthy();
  });
});
