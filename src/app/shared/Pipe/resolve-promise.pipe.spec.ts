import { ResolvePromisePipe } from './resolve-promise.pipe';

describe('ResolvePromisePipe', () => {
  it('create an instance', () => {
    const pipe = new ResolvePromisePipe();
    expect(pipe).toBeTruthy();
  });
});
